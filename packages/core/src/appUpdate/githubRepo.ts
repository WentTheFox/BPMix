/** "owner/repo" - shared between SettingsScreen's About links and the update checker, rather than a second copy drifting out of sync with the first. */
export const GITHUB_REPO = 'WentTheFox/BPMix';
export const GITHUB_REPO_URL = `https://github.com/${GITHUB_REPO}`;
